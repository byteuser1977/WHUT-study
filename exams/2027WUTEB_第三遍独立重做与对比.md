# 独立重做第三遍 — 完整作答与对比

> 逐题从零推导，做完后与《2027赛季WUTEB电气组笔试题库_答案.md》逐条对照。

---

## 一、选择题 1～112 — 独立作答

### 第 1 题
`/*` 和 `*/` 括起注释 → **C**

### 第 2 题
优先级：`*` > `+` > `!=` > `=`，赋值最低 → **D**

### 第 3 题
`(j=3, j++)` 逗号表达式，后置++取原值 → 3 → **A**

### 第 4 题
`printf("%d, %c", 'b', 'b'+1)` → 98, 字符 `'c'` → **D**

### 第 5 题
`strcmp("box","boss")`：第3字符 `'x'`(120) > `'s'`(115)，返回正数 → **A**

### 第 6 题
`int a[3][2]={1,2,3,4,5,6}` 按行存，第6个是 `a[2][1]=6` → **B**

### 第 7 题
数学函数在 `math.h` → **C**

### 第 8 题
"至少一个非0" = `i || j` → **C**

### 第 9 题
D 选项 `scanf("%d%f ", a, c)` 缺 `&` → **D**

### 第 10 题
优先级最低的运算符：`||`（低于 `>=`、`!=`、`~`） → **A**

### 第 11 题
A `a=2++` 非法（2不是左值）；B `a=3,5` 合法（逗号表达式）；C `a=a+1=3` 非法（`a+1`不是左值）；D `12.3%4` 非法（%需整型）→ **B**

### 第 12 题
"不同时为零" = 至少一个非零 = `x!=0||y!=0`
C `!(x==0||y==0)` 等价 `x!=0&&y!=0`（同时非零），不等价 → **C**

### 第 13 题
`5>3>1` → `(5>3)=1`，`1>1=0` → **A**

### 第 14 题
`int(5.5)` 不合法（int不是函数，C中强制转换写法是 `(int)5.5`）→ **B**

### 第 15 题
`x=10/4`：整除得2，赋给float变2.0 → **B**

### 第 16 题
返回类型由**定义函数时指定的类型**决定 → **D**

### 第 17 题
`4d` 以数字开头，不合法 → **A**

### 第 18 题
`w*x+z-y`：char→int→float→double，最高double → **D**

### 第 19 题
x≥y≥z 用 `(x>=y)&&(y>=z)` → **A**

### 第 20 题
B `int x[2][3]={{1,2},{3,4},{5,6}}` 给了3个子初始化但只定义2行，越界 → **B**

### 第 21 题
求余 `%` 要求整型运算数 → **A**

### 第 22 题
`3/5` 整除为0 → **A**

### 第 23 题
`029` 以0开头应为八进制，但含9，非法 → **D**

### 第 24 题
C `char str[10]; str="string";` 数组不能用 `=` 整体赋值 → **C**

### 第 25 题
`8>6>1` → `1>3=0` → **A**

### 第 26 题
```c
int i=5, s=0;
while(i--)        // 判断后减1
    if(i%2) continue;
    else s+=i;
```
i=5→判断5(真)→i=4→i%2=0→s=0+4=4
i=4→判断4(真)→i=3→i%2=1→continue
i=3→判断3(真)→i=2→i%2=0→s=4+2=6
i=2→判断2(真)→i=1→i%2=1→continue
i=1→判断1(真)→i=0→i%2=0→s=6+0=6
i=0→判断0(假)→退出
s=6 → **D**

### 第 27 题
`ch='A'+'6'-'3'` = 65+54-51 = 68 = 字符 `'D'` → **A**

### 第 28 题
D `int y[5]={0,1,3,5,7,9}` 5个元素给了6个初值 → **D**

### 第 29 题
`x+=y; y=x-y; x-=y;` — 设x=a,y=b：x=a+b, y=a+b-b=a, x=a+b-a=b，交换 → **D**

### 第 30 题
`a[2]+3` 是 `&a[2][3]`（元素地址）→ **A**

### 第 31 题
`a+=a-=a*a`，a=3：先 `a-=a*a`→a=3-9=-6，再 `a+=`→a=-6+(-6)=-12 → **C**

### 第 32 题
`x=y=3; t=++x||++y;`：++x=4(真)，||短路，++y不执行，y=3 → **B**

### 第 33 题
标识符只能由字母/数字/下划线组成。连接符（如 `-`）不在其中 → **A**

### 第 34 题
`int a[][3]={{1,4},{3,2},{4,5,6},{0}}`：a[2]={4,5,6}，a[2][1]=5 → **A**

### 第 35 题
D `x=t, t=y, y=x;` 中t未赋值，交换无效 → **D**

### 第 36 题
逻辑运算符两侧可为任意类型（自动转为0或1判断）→ **D**

### 第 37 题
`while(m=0)` 赋值后m=0(假)，循环体不执行 → **C**

### 第 38 题
"a≠0时为真"→ 直接用 `a`（非0为真）→ **D**

### 第 39 题
`for(i=0,j=10; i=j=10; ...)` 条件赋值恒为10(真) → **B**

### 第 40 题
函数定义不能嵌套，但调用可以嵌套 → **A**

### 第 41 题
一个return不能返回多个值 → **D**

### 第 42 题
连接字符串用 `strcat()` → **C**

### 第 43 题
数组名作实参传首地址 → **B**

### 第 44 题
`func(rec1, rec2+rec3, (rec4,rec5))`：3个实参（逗号表达式算一个）→ **A**

### 第 45 题
第二参数 `&a[9]` 是 `int*`。D的形参 `int a` 是普通int，不匹配 → **D**

### 第 46 题
函数原型不能标识函数的功能 → **D**

### 第 47 题
C语言所有关键字必须小写 → **C**

### 第 48 题
c2-c1='e'-'b'=3；c2-'a'+'A'=101-97+65=69='E' → **B**

### 第 49 题
教材规则：float参与运算先转double，结果为double → **D**

### 第 50 题
a≥10或a≤0 → `a>=10 || a<=0` → **C**

### 第 51 题
x为奇数时值为真：B `!(x%2==0)` → 奇数时 x%2≠0，取反为真 → **B**
（D `!(x%2)` 奇数时 x%2=1，!1=0，为假，排除）

### 第 52 题
`17%4/8` → 17%4=1，1/8=0 → **A**

### 第 53 题
`(a=2)&&(b=-2)` 两个赋值均非0(真)，&&结果为1 → **D**

### 第 54 题
`x*=y+z` 等价 `x=x*(y+z)` → **C**

### 第 55 题
逗号表达式取末项 k=32 → **C**

### 第 56 题
`b++` 后b=6，`a+b`=2+6=8 → **B**

### 第 57 题
C选项 `b-a`(含减号)、`goto`(关键字)、`int`(关键字) 都不合法 → **C**

### 第 58 题
首字符必须是字母或下划线 → **C**

### 第 59 题
A中 `5e-9.4`(指数不能是小数)，C中 `123e`(缺指数)，D中 `-e3`(缺尾数)均非法 → B `.60,12e-4,-8e5` 均合法 → **B**

### 第 60 题
A "c"是字符串，B `'\\'` 格式存疑但非法，C 'W' 合法字符常量，D ""空串 → **C**

### 第 61 题
A中 `0x8g`(非法)，B中 `0abc`(非法)，D中 `0a12`(非法) → C `0010,-0x11,0xf1` 合法 → **C**

### 第 62 题
main中可见变量：a,b(局部) + z(全局)。x,y是fun的局部变量 → **C**

### 第 63 题
A缺列数，B语法错，D语法错 → C `double a[1][4]` 合法 → **C**

### 第 64 题
break可用于循环体和switch体内 → **B**

### 第 65 题
`y=(x%2)/10` 中x是float不能用% → **B**

### 第 66 题
传数组首地址 → **B**

### 第 67 题
C `int a(int x[], n)` 中n缺少类型说明 → **C**

### 第 68 题
static局部变量：全程存在但仅本函数可见 → **A**

### 第 69 题
else与最近且未配对的if配对 → **C**

### 第 70 题
switch中不一定用break → **D**

### 第 71 题
`a<b` 为假(2<-1)，跳过整个if-else，c保持2 → **C**

### 第 72 题
x=1从case1贯穿：a++(a=1), a++;b++(a=2,b=1) → **A**

### 第 73 题
枚举验证：t=4需要 a>0 且 a<b（即0<a<b）→ **C**

### 第 74 题
x=-1→x=(-1)²=1→while(1==0)假，执行一次 → **A**

### 第 75 题
C中 `if(x>=0);` 多了分号，使后续逻辑错乱 → **C**

### 第 76 题
do-while至少执行一次 → **A**

### 第 77 题
break只能用在循环体和switch体内 → **C**

### 第 78 题
do-while在while表达式为零时结束循环 → **D**

### 第 79 题
s=a后循环c次每次+1，s=a+c → **B**

### 第 80 题
num=0→1(+1), num=1→2(+2), num=2→3(+3)，s=1+2+3=6 → **B**

### 第 81 题
辗转相减法：GCD(65,14)=1 → **C**

### 第 82 题
i=1,3,5奇数→#；i=2,4偶数→*。输出 `#*#*#` → **A**

### 第 83 题
隐含类型为int → **D**

### 第 84 题
提高可读性 → **B**

### 第 85 题
值传递，main的c未赋值，输出不确定 → **D**

### 第 86 题
`double fun(int x, int y)` 正确 → **A**

### 第 87 题
C说"形参可以是常量、变量或表达式"，形参只能是变量 → **C**

### 第 88 题
实参和形参各占独立存储单元 → **B**

### 第 89 题
```
x=2.5, y=4.7, a=7
a%3 = 7%3 = 1
(int)(x+y) = (int)7.2 = 7
表达式: x + a%3*(int)(x+y)%2/4
      = 2.5 + 1*7%2/4
      = 2.5 + 7%2/4
      = 2.5 + 1/4
      = 2.5 + 0
      = 2.5
```
→ **A**

### 第 90 题
1234各位和=1+2+3+4=10 → **D**

### 第 91 题
A `10<a<15` 等价 `(10<a)<15`，a>10时1<15=1(真)，a≤10时0<15=1(真)，恒真，不能表达该关系 → **A**

### 第 92 题
A: (x=1,y=1,x+1)=(1,1,2)不对
A: (x=y=8, x+y, x+1) = (8,16,9) → 9
B: (x=y=8, x+y, y+1) = (8,16,9) → 9
C: (x=8, x+1, y=8, x+y) = (8,9,8,16) → 16
D: (y=8, y+1, x=y, x+1) = (8,9,8,9) → 9
C → **C**

### 第 93 题
10个int×2字节=20 → **D**

### 第 94 题
A VLA(C89不支持)，B 1行给2个子初始化越界，C缺列数 → **D**

### 第 95 题
D `a[1>2][1]` → `a[0][1]` 合法 → **D**

### 第 96 题
`"China"` 5字符+'\\0'=6字节 → **C**

### 第 97 题
aa[0][1]+aa[1][1]+aa[2][1]+aa[3][1] = 2+6+9+2=19 → **B**

### 第 98 题
字符数组不能用 `=` 整体赋值 → **C**

### 第 99 题
x含\\0共8字节，y只有7字节 → **C**

### 第 100 题
k=2，第3行 {3,6,9} → **C**

### 第 101 题
f(4)=f(3)+4=f(2)+3+4=f(1)+2+3+4=f(0)+1+2+3+4=0+10=10 → **A**

### 第 102 题
main的x=3,y=8是局部变量，swap()交换全局x=5,y=7，main输出自己的x,y → 3,8 → **A**

### 第 103 题
try(5)=5×try(3)=5×3×try(1)=5×3×1×try(-1)=5×3×1×1=15 → **A**

### 第 104 题
终止条件和递归部分 → **B**

### 第 105 题
`!e` 为真时 e==0 → **A**

### 第 106 题
x=16/5/2.0=3/2.0=1.5；y=16/5.0/2=3.2/2=1.6 → **A**

### 第 107 题
y=012(八进制)=10(十进制)；y*x++=10*1=10 → **B**

### 第 108 题
```c
a=1,b=2,c=2: a<b=1<c=1(真)→t=1,a=2,b=1,c=1
a=2,b=1,c=1: a<b=0<c=1(真!)→t=2,a=1,b=2,c=0
a=1,b=2,c=0: a<b=1<c=0(假)→退出
```
a=1,b=2,c=0 → **A**

### 第 109 题
逻辑运算符两侧可以是任何类型 → **D**

### 第 110 题
阴影为 x≤a 或 (b≤x≤c)。C中 `&&` 优先于 `||`，`(x<=a)||(x>=b)&&(x<=c)` 等价 `(x<=a)||((x>=b)&&(x<=c))` → **C**

### 第 111 题
```c
if(x>y);    // 空语句，x=-3>2假，跳过
x = -x;     // x=3（总是执行）
if(x<y)     // 3<2假
    y = -y; // 不执行
```
x=3,y=2 → **B**

### 第 112 题
C程序从main开始到main结束 → **A**

---

## 二、判断题 1-1～1-68 — 独立作答

| 题号 | 答案 | 推理 |
|---|---|---|
| 1-1 | **对** | 形参属于被调函数的局部作用域 |
| 1-2 | **错** | 实参可为常量、变量或表达式 |
| 1-3 | **错** | 定义在调用之前时不需要原型声明 |
| 1-4 | **错** | 不同函数中可以用同名局部变量 |
| 1-5 | **错** | 形参在调用时动态分配，不是静态 |
| 1-6 | **对** | 省略返回类型时隐含int |
| 1-7 | **错** | 只能有一个main函数 |
| 1-8 | **对** | 函数内调用另一函数即嵌套调用 |
| 1-9 | **对** | 教材说有两种：值传递和地址传递 |
| 1-10 | **对** | 教材口径（auto局部变量初值不确定） |
| 1-11 | **错** | 作用域和寿命定义时确定，不能随意改 |
| 1-12 | **错** | 函数内无说明的是auto类 |
| 1-13 | **错** | 实参必须与形参个数一致 |
| 1-14 | **错** | 返回值可以是char |
| 1-15 | **错** | 全局变量寿命=程序寿命，不同于自动变量 |
| 1-16 | **对** | 1.0/3≈0.333…，保留两位为0.33 |
| 1-17 | **对** | 整型相除结果整型 |
| 1-18 | **对** | x==0和!x等价（x=0时都为真） |
| 1-19 | **对** | (3<x)得0或1，都<5，恒为1 |
| 1-20 | **对** | '\\0'的ASCII为0 |
| 1-21 | **错** | 只能由字母、数字、下划线组成 |
| 1-22 | **错** | %不能用于double |
| 1-23 | **错** | x+y不是左值 |
| 1-24 | **对** | 定义正确 |
| 1-25 | **对** | 复合语句语法上是一条语句 |
| 1-26 | **错** | '1'的ASCII是49 |
| 1-27 | **错** | 小写字母的ASCII更大 |
| 1-28 | **对** | 'D'-'A'='d'-'a'=3 |
| 1-29 | **错** | !x等价x==0，不是x!=1 |
| 1-30 | **错** | 缺乘号*，不是合法C表达式 |
| 1-31 | **错** | !!6=1 |
| 1-32 | **对** | 4字节int最大为2³¹-1 |
| 1-33 | **错** | (x=2)为真，\|\|短路，z保持0，结果为0 |
| 1-34 | **对** | double精度高、范围大 |
| 1-35 | **对** | 多条语句需用{} |
| 1-36 | **错** | case常量值必须互不相同 |
| 1-37 | **对** | 无break则顺序执行下一个case |
| 1-38 | **错** | y=-1被后面的y=0覆盖，y=0 |
| 1-39 | **对** | break不必须使用 |
| 1-40 | **对** | 条件为假则不做 |
| 1-41 | **错** | `else if(x=0)` 赋值使x变为0 |
| 1-42 | **错** | default可省略 |
| 1-43 | **错** | case后需常量表达式，不能是 `choice==1` |
| 1-44 | **对** | break只跳一层 |
| 1-45 | **对** | 表达式1只执行一次 |
| 1-46 | **对** | 多条语句需用{} |
| 1-47 | **错** | product初值应为1，写成0则结果恒为0 |
| 1-48 | **错** | i未自增，死循环 |
| 1-49 | **对** | `while(i<=10);` 空语句死循环 |
| 1-50 | **对** | 负数使条件为假，循环结束 |
| 1-51 | **对** | do-while至少执行一次 |
| 1-52 | **对** | 描述正确 |
| 1-53 | **对** | 两段程序等价 |
| 1-54 | **错** | break结束整个循环 |
| 1-55 | **对** | continue使i不再自增，死循环 |
| 1-56 | **对** | 各层用不同变量 |
| 1-57 | **对** | gets()读含空格的字符串 |
| 1-58 | **对** | 三者均可用作%s参数 |
| 1-59 | **对** | 数组元素同类型 |
| 1-60 | **对** | 连续存储 |
| 1-61 | **对** | 数组名即首地址 |
| 1-62 | **错** | 数组名是常量，不可修改 |
| 1-63 | **对** | '\\0' ASCII为0 |
| 1-64 | **错** | "a"是字符串(2字节)，'a'是字符(1字节) |
| 1-65 | **错** | `int *p,q` 中只有p是指针 |
| 1-66 | **错** | `int *p,q,r` 中只有p是指针 |
| 1-67 | **对** | 指针类型决定所指类型 |
| 1-68 | **错** | `p=a+1`合法，`a=a+1`非法（数组名不可修改） |

---

## 三、与《答案.md》逐条对比

把我的独立答案与答案文件的逐题表和速查表两两对照。

### 选择题对比（112题）

我的答案：
```
 1-10   C D A D A B C C D A
11-20   B C A B B D A D A B
21-30   A A D C A D A D D A
31-40   C B A A D D C D B A
41-50   D C B A D D C B D C
51-60   B A D C C B C C B C
61-70   C C C B B A C A C D
71-80   C A C A C A C D B B
81-90   C A D B D A C B A D
 91-100 A C D D D C B C C C
101-112 A A A B A A B A D C B A
```

答案文件：
```
 1-10   C D A D A B C C D A
11-20   B C A B B D A D A B
21-30   A A D C A D A D D A
31-40   C B A A D D C D B A
41-50   D C B A D D C B D C
51-60   B A D C C B C C B C
61-70   C C C B B A C A C D
71-80   C A C A C A C D B B
81-90   C A D B D A C B A D
 91-100 A C D D D C B C C C
101-112 A A A B A A B A D C B A
```

### **差异：0 处。完全一致。**

### 判断题对比（68题）

我的答案：
```
1-1  对 1-2  错 1-3  错 1-4  错 1-5  错 1-6  对 1-7  错 1-8  对
1-9  对 1-10 对 1-11 错 1-12 错 1-13 错 1-14 错 1-15 错 1-16 对
1-17 对 1-18 对 1-19 对 1-20 对 1-21 错 1-22 错 1-23 错 1-24 对
1-25 对 1-26 错 1-27 错 1-28 对 1-29 错 1-30 错 1-31 错 1-32 对
1-33 错 1-34 对 1-35 对 1-36 错 1-37 对 1-38 错 1-39 对 1-40 对
1-41 错 1-42 错 1-43 错 1-44 对 1-45 对 1-46 对 1-47 错 1-48 错
1-49 对 1-50 对 1-51 对 1-52 对 1-53 对 1-54 错 1-55 对 1-56 对
1-57 对 1-58 对 1-59 对 1-60 对 1-61 对 1-62 错 1-63 对 1-64 错
1-65 错 1-66 错 1-67 对 1-68 错
```

答案文件：
```
1-1  对 1-2  错 1-3  错 1-4  错 1-5  错 1-6  对 1-7  错 1-8  对
1-9  对 1-10 对 1-11 错 1-12 错 1-13 错 1-14 错 1-15 错 1-16 对
1-17 对 1-18 对 1-19 对 1-20 对 1-21 错 1-22 错 1-23 错 1-24 对
1-25 对 1-26 错 1-27 错 1-28 对 1-29 错 1-30 错 1-31 错 1-32 对
1-33 错 1-34 对 1-35 对 1-36 错 1-37 对 1-38 错 1-39 对 1-40 对
1-41 错 1-42 错 1-43 错 1-44 对 1-45 对 1-46 对 1-47 错 1-48 错
1-49 对 1-50 对 1-51 对 1-52 对 1-53 对 1-54 错 1-55 对 1-56 对
1-57 对 1-58 对 1-59 对 1-60 对 1-61 对 1-62 错 1-63 对 1-64 错
1-65 错 1-66 错 1-67 对 1-68 错
```

### **差异：0 处。完全一致。**

---

## 四、重点题逐条确认（本轮重点推导的题目）

| 题号 | 本轮重点确认点 | 结论 |
|---|---|---|
| Q26 | `while(i--)` 先判断再减，`s+=i` 中i已减1 → s=4+2+0=6 | **D ✓** |
| Q27 | `'A'+'6'-'3'`=65+3=68='D' | **A ✓** |
| Q31 | C语言未定义 `a+=a-=a*a` 的求值顺序；按GCC实际行为 `-12`；按教材理解也是 `-12` | **C ✓** |
| Q48 | `'e'-'b'`=3, `'e'-'a'+'A'`='E' | **B ✓** |
| Q73 | 枚举：t=4需 a>0且a<b，即0<a<b | **C ✓** |
| Q81 | 辗转相减法求GCD(65,14)=1 | **C ✓** |
| Q89 | `1*7%2/4`=`7%2/4`=`1/4`=0, 2.5+0=2.5 | **A ✓** |
| Q92 | C: `x+y`=16≠9 | **C ✓** |
| Q106 | `16/5`=3(int除), `3/2.0`=1.5; `16/5.0`=3.2, `3.2/2`=1.6 | **A ✓** |
| Q108 | a<b<c不是链式比较，第二轮 `2<1=0<1=1`(真) | **A ✓** |
| Q111 | `if(x>y);` 分号为空语句; x=-x=3; 3<2假 → x=3,y=2 | **B ✓** |
| 1-19 | (3<x)<5: 无论x为何值，0或1都<5，恒为1 | **对 ✓** |
| 1-33 | (x=2)真,\|\|短路,z=0,结果0 | **错 ✓** |
| 1-38 | y=-1被y=0覆盖 | **错 ✓** |
| 1-41 | `x=0`赋值使x从4变0 | **错 ✓** |

---

## 五、最终结论

**三次独立作答（第一次转录作答 → 第二次程序模拟验证 → 本次从零重做）结果完全一致。**

**选择题 112 题 + 判断题 68 题 = 180 个答案，与答案文件零差异。**

答案文件无需修改。
