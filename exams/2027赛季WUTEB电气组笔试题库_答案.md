# 2027 赛季 WUTEB 电气组笔试题库 · 参考答案

> 对应题目文档：《2027赛季WUTEB电气组笔试题库.md》
> 格式：**题号 → 答案**，附简要解析（选择题/判断题）。实验题给出完整参考代码（C 语言）。

---

## 一、选择题答案（1～112）

| 题号 | 答案 | 解析 |
|---|---|---|
| 1 | **C** | 注释必须用 `/*` 和 `*/` 括起来 |
| 2 | **D** | `=` 赋值运算符优先级最低（低于 `+`、`!=`、`*`） |
| 3 | **A** | 逗号表达式取最后一个表达式的值，`j++` 为后置自增，值取 3 |
| 4 | **D** | `'b'` 输出 98；`'b'+1` = 99，以 `%c` 输出为字符 `'c'` |
| 5 | **A** | `"box"` 与 `"boss"` 第 3 个字符 `'x'(120)` > `'s'(115)`，返回正数 |
| 6 | **B** | 二维数组按行存放，`a[2][1]` 为第 6 个元素，值为 6 |
| 7 | **C** | 数学函数声明在 `math.h` 中 |
| 8 | **C** | “至少一个非 0”即逻辑或 `i \|\| j` |
| 9 | **D** | 整型和实型都要用取地址符，D 缺 `&` |
| 10 | **A** | `\|\|` 优先级最低（`~` 最高，其次 `!=`、`>=`） |
| 11 | **B** | `a=3,5` 是合法逗号表达式；`2++`、`a+1=3`、`12.3%4` 均非法 |
| 12 | **C** | `!(x==0 \|\| y==0)` 等价于 `x!=0 && y!=0`，是“同时非 0”，不能表示“不同时为零”；D 才是正确表达 |
| 13 | **A** | 左结合：`(5>3)` 得 1，`1>1` 得 0 |
| 14 | **B** | `int` 不是函数，`int(5.5)` 不是合法的强制类型转换写法 |
| 15 | **B** | `10/4` 为整数除法得 2，赋给浮点型 x 后为 2.0 |
| 16 | **D** | 函数返回类型由定义函数时指定的类型决定（return 表达式会按此转换） |
| 17 | **A** | 标识符不能以数字开头 |
| 18 | **D** | 混合运算按“级别最高者”决定：double 级别最高 |
| 19 | **A** | C 无 `AND` 与连续比较写法，用 `&&` 连接两个关系表达式 |
| 20 | **B** | `int x[2][3]` 只有 2 行，却给了 3 行初值（另：C 选项 `(4,5,6)` 用了圆括号，原文即是笔误） |
| 21 | **A** | 求余运算符 `%` 的运算数必须是整型 |
| 22 | **A** | `3/5` 为整数除法，结果为 0 |
| 23 | **D** | 以 0 开头的八进制数中不允许出现 9，故为非法数 |
| 24 | **C** | 字符数组不能用赋值语句整体赋值（只能用 `strcpy` 或初始化） |
| 25 | **A** | `8>6` 得 1，`1>3` 得 0 |
| 26 | **D** | i 依次为 5,4,3,2,1,0：i=4 时 s+=4；i=2 时 s+=2；i=0 时 s+=0 → s=6 |
| 27 | **A** | `'A'+'6'-'3'` = 65+3 = 68，即字符 `'D'`（选项 B 的 68 是同一值的数值形式） |
| 28 | **D** | `int y[5]` 只能有 5 个初值，给了 6 个 |
| 29 | **D** | `x+=y; y=x-y; x-=y;` 实现两数交换 |
| 30 | **A** | `a[2]` 是行地址，`+3` 后为 `&a[2][3]`，是元素地址 |
| 31 | **C** | 先算 `a-=a*a`：a = 3-9 = -6；再算 `a+=` ：a = -6 + (-6) = -12 |
| 32 | **B** | `++x` 为 4（真），`\|\|` 短路，`++y` 不再执行，y 保持 3 |
| 33 | **A** | 连接符（如 `-`）不能出现在标识符中；下划线、字母、数字均可（数字不可开头） |
| 34 | **A** | 第三行 `{4,5,6}`，`a[2][1]` = 5 |
| 35 | **D** | `x=t, t=y, y=x;` 中 t 未初始化，交换结果错误 |
| 36 | **D** | 逻辑运算符两侧可以是任何类型的数据（按“非 0 为真”处理） |
| 37 | **C** | `while(m=0)` 中赋值表达式的值为 0（假），循环体一次也不执行 |
| 38 | **D** | `a` 本身在 a≠0 时为真；`a=0` 是赋值且值为 0 |
| 39 | **B** | 表达式 2 为 `i=j=10`，赋值后恒为 10（真），故无限次 |
| 40 | **A** | C 语言函数定义不能嵌套，但调用可以嵌套 |
| 41 | **D** | 一个 return 语句只能返回一个值 |
| 42 | **C** | `strcat` 连接字符串 |
| 43 | **B** | 数组名作实参传递的是数组首地址 |
| 44 | **A** | 三个实参：`rec1`、`rec2+rec3`、`(rec4,rec5)`（逗号表达式算一个） |
| 45 | **D** | 实参 `&a[9]` 是地址，形参不能是普通 int 变量 |
| 46 | **D** | 函数原型只声明返回类型、参数个数与类型，不标识函数功能 |
| 47 | **C** | C 语言所有关键字必须小写（其他标识符大小写敏感） |
| 48 | **B** | `c2-c1` = 3；`c2-'a'+'A'` = 4+65 = 69，即 `'E'` |
| 49 | **D** | 按教材转换规则，float 型数据参与运算时先转换为 double，结果为 double |
| 50 | **C** | C 语言中“或”用 `\|\|` |
| 51 | **B** | `!(x%2==0)`：x 为奇数时 `x%2` 非 0，条件为假取反后为真 |
| 52 | **A** | 同级从左到右：`17%4` = 1，`1/8` = 0 |
| 53 | **D** | `(a=2)&&(b=-2)` 两个赋值表达式均为真，逻辑与结果为 1 |
| 54 | **C** | 复合赋值等价于 `x = x * (y + z)` |
| 55 | **C** | 逗号表达式取最后一个 `k=32` |
| 56 | **B** | `b++` 后 b=6，`a+b` = 2+6 = 8 |
| 57 | **C** | `b-a` 含减号非法，`goto`、`int` 是关键字；A 中 `A` 合法，B 中 `_A` 合法，D 全合法 |
| 58 | **C** | 首字符必须是字母或下划线 |
| 59 | **B** | `-.60`、`12e-4`、`-8e5` 均为合法浮点数（A 中 `5e-9.4`、C 中 `123e`/`1.2e-.4`、D 中 `-e3` 非法） |
| 60 | **C** | 字符常量用单引号；B 的写法不合规范 |
| 61 | **C** | `0010`（八进制）、`-0x11`、`0xf1`（十六进制）均合法（A 中 `0x8g`、B 中 `0abc`、D 中 `0a12` 非法） |
| 62 | **C** | main 中可用的是局部变量 a、b 和全局变量 z；x、y 属于 fun |
| 63 | **C** | 二维数组定义必须给出除第一维外的所有维长度 |
| 64 | **B** | break 可用于循环体和 switch 体内 |
| 65 | **B** | `%` 的运算数必须是整型，x 为 float，非法 |
| 66 | **A** | 传递的是数组首地址 |
| 67 | **C** | 形参中 `n` 缺少类型说明（`int a(int x[], n)` 不完整） |
| 68 | **A** | 全程存在但仅在本函数内可见 → 静态（static）局部变量 |
| 69 | **C** | else 与之前最近的、尚未配对的 if 配对 |
| 70 | **D** | switch 中 break 不是必需的（视需要决定） |
| 71 | **C** | `a<b` 为假，整个 if 不执行，c 仍为 2 |
| 72 | **A** | x=1 从 `case 1` 开始向下贯穿：a++、a++、b++ → a=2, b=1 |
| 73 | **C** | 要 `t=2*s=4` 需 s=2（即 a>0）且不满足 `a>b`、`a!=b` → 0<a<b |
| 74 | **A** | x=-1 → x=1；`while(1==0)` 为假，循环体只执行一次 |
| 75 | **C** | C 中 `if(x >= 0);` 后面多了分号，逻辑错误（y 永远被赋 -1） |
| 76 | **A** | do-while 的循环体至少无条件执行一次 |
| 77 | **C** | break 只能用在循环体内和 switch 语句体内 |
| 78 | **D** | do-while 在 while 后表达式为 0（假）时结束循环 |
| 79 | **B** | 循环执行 c 次，每次 s 加 1 → `s = a + c` |
| 80 | **B** | num 依次 1、2、3 累加：1+2+3 = 6 |
| 81 | **C** | 程序是辗转相除法求最大公约数，gcd(65,14) = 1 |
| 82 | **A** | i=1,2,3,4,5 → 输出 `# * # * #` |
| 83 | **D** | 省略返回类型时隐含为 int（C89/C90 规则） |
| 84 | **B** | 建立自定义函数的主要目的之一是提高程序的可读性 |
| 85 | **D** | 参数是值传递，主调函数中的 c 未被赋值，输出无法确定 |
| 86 | **A** | 函数定义形参之间用逗号分隔，形参必须逐个声明类型，末尾不加分号 |
| 87 | **C** | 形参不可能是常量或表达式，只能是变量（或数组/指针） |
| 88 | **B** | 实参与形参各自占用独立的存储单元 |
| 89 | **A** | `a%3`=1，`(int)(x+y)`=7，`1*7%2/4` = `1/4` = 0，x+0 = 2.5 |
| 90 | **D** | 求各位数字之和：1+2+3+4 = 10 |
| 91 | **A** | `10<a<15` 是 `(10<a)<15`，恒为真，不能正确表达 |
| 92 | **C** | `(x=8, x+1, y=8, x+y)` 最后一项 x+y = 8+8 = 16 ≠ 9 |
| 93 | **D** | 10 个元素 × 2 字节 = 20 字节（与初值个数无关） |
| 94 | **D** | `int d[3][2]={{1,2},{34}};` 合法（部分赋初值，其余补 0）；B 的行数超出，C 缺列长度，A 缺常量维度 |
| 95 | **D** | `1>2` 得 0，即 `a[0][1]`，合法；A、B、C 均越界 |
| 96 | **C** | `"China"` 5 个字符 + 结束符 `'\0'` = 6 字节 |
| 97 | **B** | 取每行第 2 列：2+6+9+2 = 19 |
| 98 | **C** | 字符数组不能用 `=` 整体赋值 |
| 99 | **C** | x 含结束符共 8 字节，y 只有 7 字节，x 长度更大 |
| 100 | **C** | k=2，取第 3 行 `{3,6,9}` |
| 101 | **A** | f(4)=4+f(3)=4+3+2+1+f(0)=10 |
| 102 | **A** | 局部变量 x、y 屏蔽全局变量，swap 交换的是全局量，输出仍为 3 , 8 |
| 103 | **A** | try(5)=5*try(3)=5*3*try(1)=5*3*1*try(-1)=15 |
| 104 | **B** | 递归算法必须有终止条件和递归部分（递归出口 + 递归体） |
| 105 | **A** | `!e` 为真即 e 为 0，等价于 `e==0` |
| 106 | **A** | `16/5`=3（整除），`3/2.0`=1.5 → x=1.5；`16/5.0`=3.2，`3.2/2`=1.6 → y=1.6 |
| 107 | **B** | `012` 是八进制 10，`10*1` = 10 |
| 108 | **A** | 迭代: (1<2)=1<2 真→a=2,b=1,c=1；(2<1)=0<1 真→a=1,b=2,c=0；(1<2)=1<0 假 → 结束，输出 1,2,0 |
| 109 | **D** | 可以是任何类型的数据 |
| 110 | **C** | 阴影为 `x<=a` 或 `b<=x<=c`，即 `(x<=a)\|\|(x>=b)&&(x<=c)`（`&&` 优先于 `\|\|`） |
| 111 | **B** | `if(x>y);` 后是空语句；x 取反为 3；`3<2` 为假，y 仍为 2 |
| 112 | **A** | C 程序从 main 开始，到 main 结束 |

**答案速查（1～112）：**

```
1-10   C D A D A B C C D A
11-20  B C A B B D A D A B
21-30  A A D C A D A D D A
31-40  C B A A D D C D B A
41-50  D C B A D D C B D C
51-60  B A D C C B C C B C
61-70  C C C B B A C A C D
71-80  C A C A C A C D B B
81-90  C A D B D A C B A D
91-100 A C D D D C B C C C
101-112 A A A B A A B A D C B A
```

---

## 二、判断题答案（1-1 ～ 1-68）

| 题号 | 答案 | 解析 |
|---|---|---|
| 1-1 | **对** | 形参属于被调函数内的局部变量 |
| 1-2 | **错** | 实参可以是常量、变量或表达式 |
| 1-3 | **错** | 若函数定义在调用之前，可不加原型声明 |
| 1-4 | **错** | 不同函数中可以使用同名的局部变量 |
| 1-5 | **错** | 形参是动态分配（随函数调用建立、返回时释放） |
| 1-6 | **对** | 省略返回类型说明时隐含为 int |
| 1-7 | **错** | 可以有多个函数，但只能有一个 main 函数 |
| 1-8 | **对** | 这就是嵌套调用 |
| 1-9 | **对** | 教材说法：值传递与地址传递两种方式 |
| 1-10 | **对** | 自动（auto）局部变量未赋初值时其值不确定（静态局部变量除外，默认为 0） |
| 1-11 | **错** | 作用域与寿命在定义时确定，不能随意修改 |
| 1-12 | **错** | 函数内未加说明的是 auto 类，函数外的是外部变量 |
| 1-13 | **错** | 实参个数必须与形参个数一致（否则出错） |
| 1-14 | **错** | 返回值可以是 char 类型 |
| 1-15 | **错** | 全局变量寿命与程序相同，作用域为整个程序，与自动变量不同 |
| 1-16 | **对** | 1.0/3 = 0.3333…，保留两位小数为 0.33 |
| 1-17 | **对** | 两个整型数相除，结果仍是整型 |
| 1-18 | **对** | x 为 0 时两式同为真，否则同为假 |
| 1-19 | **对** | `(3<x<5)` 即 `(3<x)<5`：x>3 时得 `1<5` 为 1，x≤3 时得 `0<5` 为 1，故对任意整数 x 其值恒为 1，说法成立（注意：该表达式并不能表示数学区间 3<x<5） |
| 1-20 | **对** | `'\0'` 的 ASCII 码为 0 |
| 1-21 | **错** | 标识符只能由字母、数字、下划线组成，且不能以数字开头 |
| 1-22 | **错** | 求余运算要求整型，x 为 double 不能做 `%` |
| 1-23 | **错** | `x + y = 22` 左边不是可赋值变量（左值） |
| 1-24 | **对** | `==` 为关系运算符，`=` 为赋值运算符 |
| 1-25 | **对** | 复合语句在语法上视为一条语句 |
| 1-26 | **错** | `'1'` 的 ASCII 码为 49，不是 1 |
| 1-27 | **错** | 大写字母的 ASCII 码值小于小写字母 |
| 1-28 | **对** | `'D'-'A'` = 3，`'d'-'a'` = 3，相等 |
| 1-29 | **错** | `!x` 等价于 `x == 0`，而非 `x != 1` |
| 1-30 | **错** | 缺少乘号，乘号不能省略 |
| 1-31 | **错** | `!!6` 的值为 1 |
| 1-32 | **对** | 4 字节 int 的范围为 −2^31 ~ 2^31−1 |
| 1-33 | **错** | `(x=2)` 为真，`\|\|` 短路，z 保持 0，整个表达式值为 0 |
| 1-34 | **对** | double 精度更高、取值范围更大 |
| 1-35 | **对** | 多条语句必须用 `{}` 组成复合语句 |
| 1-36 | **错** | 各 case 常量表达式必须互不相同 |
| 1-37 | **对** | 无 break 时继续执行后面的 case |
| 1-38 | **错** | 最后 `y=0` 覆盖了 `y=-1`，y 的值为 0 |
| 1-39 | **对** | break 不是必须的 |
| 1-40 | **对** | 省略 else 时条件为假则什么都不做 |
| 1-41 | **错** | `else if (x = 0)` 是赋值表达式，x 被改为 0，x 的值发生了变化 |
| 1-42 | **错** | default 可以省略 |
| 1-43 | **错** | case 后必须是常量表达式，不能是关系表达式 `choice == 1` |
| 1-44 | **对** | break 只能跳出当前这一层循环/switch |
| 1-45 | **对** | 表达式 1 只在循环开始时执行一次 |
| 1-46 | **对** | 循环体多条语句须用 `{}` 组成复合语句 |
| 1-47 | **错** | `product` 初值应为 1，赋 0 会使结果恒为 0 |
| 1-48 | **错** | 循环体中 i 未改变，会死循环，i 不会变成 11 |
| 1-49 | **对** | `while(i<=10);` 后是空语句，i 永不变化，死循环 |
| 1-50 | **对** | 输入负数时 `score>=0` 为假，循环结束 |
| 1-51 | **对** | do-while 先执行循环体再判断条件，至少执行一次 |
| 1-52 | **对** | 这是 while 与 do-while 的主要区别 |
| 1-53 | **对** | 两段程序功能相同，均为求 1+2+…+10 |
| 1-54 | **错** | break 结束整个循环，continue 只结束本次循环 |
| 1-55 | **对** | i>10 后 `continue` 使 i 不再增加，形成死循环 |
| 1-56 | **对** | 各层应使用不同变量，避免相互干扰 |
| 1-57 | **对** | `gets()` 可读入含空格的字符串（现已被标注为不安全，建议用 fgets） |
| 1-58 | **对** | 三者都可作为 `%s` 的输出参数 |
| 1-59 | **对** | 数组元素类型相同，用数组名加下标区分 |
| 1-60 | **对** | 数组元素占用一段连续的内存单元 |
| 1-61 | **对** | 数组名代表数组首地址 |
| 1-62 | **错** | 数组名是地址常量，不能被修改 |
| 1-63 | **对** | `'\0'` 的 ASCII 码值为 0 |
| 1-64 | **错** | `"a"` 是字符串（占 2 字节），`'a'` 是字符（占 1 字节） |
| 1-65 | **错** | `int *p, q;` 中只有 p 是指针，q 是 int 变量 |
| 1-66 | **错** | 只有 p 是指针，q、r 是 int 变量 |
| 1-67 | **对** | 指针类型确定后只能指向该类型的变量（可强制转换，但不推荐） |
| 1-68 | **错** | `p=a+1;` 合法，`a=a+1;` 非法（数组名不可修改） |

**答案速查（1-1 ～ 1-68）：**

```
1-1  对   1-2  错   1-3  错   1-4  错   1-5  错   1-6  对   1-7  错   1-8  对
1-9  对   1-10 对   1-11 错   1-12 错   1-13 错   1-14 错   1-15 错   1-16 对
1-17 对   1-18 对   1-19 对   1-20 对   1-21 错   1-22 错   1-23 错   1-24 对
1-25 对   1-26 错   1-27 错   1-28 对   1-29 错   1-30 错   1-31 错   1-32 对
1-33 错   1-34 对   1-35 对   1-36 错   1-37 对   1-38 错   1-39 对   1-40 对
1-41 错   1-42 错   1-43 错   1-44 对   1-45 对   1-46 对   1-47 错   1-48 错
1-49 对   1-50 对   1-51 对   1-52 对   1-53 对   1-54 错   1-55 对   1-56 对
1-57 对   1-58 对   1-59 对   1-60 对   1-61 对   1-62 错   1-63 对   1-64 错
1-65 错   1-66 错   1-67 对   1-68 错
```

---

## 三、实验题参考代码

### 1. 计算整数各位数字之和

```c
#include <stdio.h>

int main(void)
{
    unsigned n, s = 0;
    scanf("%u", &n);
    s = n % 10 + n / 10 % 10 + n / 100;
    printf("%u\n", s);
    return 0;
}
```

### 2. 逆序的三位数

```c
#include <stdio.h>

int main(void)
{
    int n;
    scanf("%d", &n);
    /* 个位×100 + 十位×10 + 百位，自然去掉前导 0 */
    printf("%d\n", n % 10 * 100 + n / 10 % 10 * 10 + n / 100);
    return 0;
}
```

### 3. 计算代数表达式

```c
#include <stdio.h>
#include <math.h>

#define PI 3.14159

int main(void)
{
    double x, y;
    double a = (sin(60.0 / 180 * PI) + 1) * (sin(30.0 / 180 * PI) + 1);

    scanf("%lf", &x);
    y = sqrt(a / cos(x / 180 * PI));
    printf("%.2f\n", y);
    return 0;
}
```

### 4. 计算分段函数

```c
#include <stdio.h>
#include <math.h>

int main(void)
{
    double x, result;

    scanf("%lf", &x);
    if (x >= 0)
        result = sqrt(x);
    else
        result = pow(x + 1, 2) + 2 * x + 1 / x;

    printf("f(%.2f) = %.2f\n", x, result);
    return 0;
}
```

### 5. A 除以 B

```c
#include <stdio.h>

int main(void)
{
    int a, b;
    scanf("%d %d", &a, &b);

    if (b > 0)
        printf("%d/%d=%.2f\n", a, b, (double)a / b);
    else if (b < 0)
        printf("%d/(%d)=%.2f\n", a, b, (double)a / b);
    else
        printf("%d/%d=Error\n", a, b);

    return 0;
}
```

### 6. 后天

```c
#include <stdio.h>

int main(void)
{
    int d;
    scanf("%d", &d);
    /* 1~7 循环：(d+1)%7+1 */
    printf("%d\n", (d + 1) % 7 + 1);
    return 0;
}
```

### 7. 求交错序列前 N 项和

```c
#include <stdio.h>

int main(void)
{
    int n, i;
    double s = 0, sign = 1;

    scanf("%d", &n);
    for (i = 1; i <= n; i++) {
        s += sign * i / (2.0 * i - 1);
        sign = -sign;
    }
    printf("%.3f\n", s);
    return 0;
}
```

### 8. 猴子吃桃问题

```c
#include <stdio.h>

int main(void)
{
    int n, i;
    int x = 1;              /* 第 N 天早上剩 1 个 */

    scanf("%d", &n);
    for (i = 1; i < n; i++) /* 从第 N 天倒推到第 1 天 */
        x = (x + 1) * 2;

    printf("%d\n", x);
    return 0;
}
```

### 9. 寻找 250

```c
#include <stdio.h>

int main(void)
{
    int x, cnt = 0;

    while (scanf("%d", &x) == 1) {
        cnt++;
        if (x == 250)
            break;
    }
    printf("%d\n", cnt);
    return 0;
}
```

### 10. 将数组中的数逆序存放

```c
#include <stdio.h>

int main(void)
{
    int n, a[10], i;

    scanf("%d", &n);
    for (i = 0; i < n; i++)
        scanf("%d", &a[i]);

    for (i = n - 1; i >= 0; i--) {
        printf("%d", a[i]);
        if (i > 0)
            printf(" ");
    }
    printf("\n");
    return 0;
}
```

### 11. 简化的插入排序

```c
#include <stdio.h>

int main(void)
{
    int n, i, x, a[11];

    scanf("%d", &n);
    for (i = 0; i < n; i++)
        scanf("%d", &a[i]);
    scanf("%d", &x);

    for (i = n - 1; i >= 0 && a[i] > x; i--)   /* 从后往前找插入位置 */
        a[i + 1] = a[i];
    a[i + 1] = x;

    for (i = 0; i <= n; i++)                   /* 每个数字后跟一个空格 */
        printf("%d ", a[i]);
    printf("\n");
    return 0;
}
```

### 12. 查询水果价格

```c
#include <stdio.h>

int main(void)
{
    int code, count = 0;

    printf("[1] apple\n");
    printf("[2] pear\n");
    printf("[3] orange\n");
    printf("[4] grape\n");
    printf("[0] exit\n");

    while (count < 5 && scanf("%d", &code) == 1) {
        if (code == 0)
            break;
        count++;
        switch (code) {
            case 1: printf("price = 3.00\n");  break;
            case 2: printf("price = 2.50\n");  break;
            case 3: printf("price = 4.10\n");  break;
            case 4: printf("price = 10.20\n"); break;
            default: printf("price = 0.00\n"); break;
        }
    }
    return 0;
}
```

### 13. 矩阵运算

```c
#include <stdio.h>

int main(void)
{
    int n, i, j, x, s = 0;

    scanf("%d", &n);
    for (i = 0; i < n; i++)
        for (j = 0; j < n; j++) {
            scanf("%d", &x);
            /* 排除最后一行、最后一列和副对角线(i+j==n-1) */
            if (i != n - 1 && j != n - 1 && i + j != n - 1)
                s += x;
        }
    printf("%d\n", s);
    return 0;
}
```

### 14. IP 地址转换

```c
#include <stdio.h>

int main(void)
{
    char s[40];
    int i, j, v;

    scanf("%s", s);
    for (i = 0; i < 4; i++) {
        v = 0;
        for (j = 0; j < 8; j++)          /* 每 8 位二进制换算成一个十进制数 */
            v = v * 2 + (s[i * 8 + j] - '0');
        printf("%d", v);
        if (i < 3)
            printf(".");
    }
    printf("\n");
    return 0;
}
```

### 15. 找两个数中最大者（函数实现）

```c
int max(int a, int b)
{
    return a > b ? a : b;
}
```

### 16. 简单实现 x 的 n 次方（函数实现）

```c
double mypow(double x, int n)
{
    double result = 1.0;
    int i;

    for (i = 0; i < n; i++)
        result *= x;
    return result;
}
```

### 17. 简单求和（函数实现）

```c
int Sum(int List[], int N)
{
    int s = 0, i;

    for (i = 0; i < N; i++)
        s += List[i];
    return s;
}
```

---

## 附：答案与解析中的几点说明

1. **第 20 题**：原题选项 C 写成 `int x[][3]={{1,2,3},(4,5,6)};`（用了圆括号），按原文也应算错误；但本题标准答案取 B（行数超出定义）。
2. **第 23 题**：题目原文为“数字 029”，因以 0 开头且含 9，不是合法的八进制常量。
3. **第 26 题**：原题表述为“Y 的值”，实际考核的是程序中的变量 s。
4. **第 49 题**：按 C 标准，`int + long + float` 的结果类型是 float；但按国内教材（谭浩强等）的“float 参与运算先转 double”规则，答案取 double。考试时按教材规则选 D。
5. **判断题 1-10**：严格来说 static 局部变量未赋初值时为 0（确定值），该说法只对 auto 变量成立；按教材习惯判为“对”。
6. **判断题 1-19**：`3<x<5` 在 C 中被解析为 `(3<x)<5`，结果对任何 x 都是 1，因此命题“值恒为 1”成立，判“**对**”。但要注意该表达式**不能**用来表示数学区间 3<x<5（`x=100` 也会“成立”）。
7. **复核说明**：所有“看程序写输出”的题目（第 4、13、15、22、25、26、27、31、32、34、37、39、48、52、53、55、56、71～74、79～82、85、89、90、92、97、100～103、106～108、111 题）均已用程序逐条模拟复核，结论与原答案一致。
